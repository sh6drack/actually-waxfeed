From 87117b6fd363e04aedc46b3902e874ad87a387e3 Mon Sep 17 00:00:00 2001
From: sh6drack <sh6drack@users.noreply.github.com>
Date: Sun, 25 Jan 2026 18:34:29 -0500
Subject: [PATCH 34/50] fix: minor styling fixes for stats page and turbopack
 config

---
 next.config.ts                      | 3 +++
 src/app/u/[username]/stats/page.tsx | 6 ++++--
 2 files changed, 7 insertions(+), 2 deletions(-)

diff --git a/next.config.ts b/next.config.ts
index 2702a7f..60d25f0 100644
--- a/next.config.ts
+++ b/next.config.ts
@@ -1,6 +1,9 @@
 import type { NextConfig } from "next";
 
 const nextConfig: NextConfig = {
+  turbopack: {
+    root: process.cwd(),
+  },
   images: {
     remotePatterns: [
       {
diff --git a/src/app/u/[username]/stats/page.tsx b/src/app/u/[username]/stats/page.tsx
index fb0cf7b..1cb9bec 100644
--- a/src/app/u/[username]/stats/page.tsx
+++ b/src/app/u/[username]/stats/page.tsx
@@ -187,9 +187,10 @@ export default async function UserStatsPage({ params }: Props) {
                 <div key={month} className="flex-1 flex flex-col items-center h-full">
                   <div className="flex-1 flex items-end w-full">
                     <div
-                      className="w-full bg-[--accent] transition-all"
+                      className="w-full transition-all"
                       style={{
                         height: count > 0 ? `${Math.max(heightPercent, 3)}%` : "0",
+                        backgroundColor: "var(--accent)",
                       }}
                     />
                   </div>
@@ -213,9 +214,10 @@ export default async function UserStatsPage({ params }: Props) {
                 <div key={i} className="flex-1 flex flex-col items-center h-full">
                   <div className="flex-1 flex items-end w-full">
                     <div
-                      className="w-full bg-[--accent] transition-all"
+                      className="w-full transition-all"
                       style={{
                         height: count > 0 ? `${Math.max(heightPercent, 4)}%` : "0",
+                        backgroundColor: "var(--accent)",
                       }}
                     />
                   </div>
-- 
2.50.1 (Apple Git-155)

