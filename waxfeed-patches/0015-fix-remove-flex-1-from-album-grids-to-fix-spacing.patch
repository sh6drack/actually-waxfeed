From b68ed050432e2cc454faa3cadb86deb7b46605ea Mon Sep 17 00:00:00 2001
From: Shadrack Annor <shadrack@mac.mynetworksettings.com>
Date: Sat, 24 Jan 2026 11:58:17 -0500
Subject: [PATCH 15/50] fix: remove flex-1 from album grids to fix spacing

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
---
 src/app/page.tsx | 30 +++++++++++++++++++-----------
 1 file changed, 19 insertions(+), 11 deletions(-)

diff --git a/src/app/page.tsx b/src/app/page.tsx
index cc6416f..395a376 100644
--- a/src/app/page.tsx
+++ b/src/app/page.tsx
@@ -255,14 +255,15 @@ export default async function Home() {
               </h2>
             </div>
 
-            {/* Mobile: 20 albums, 3-4 cols */}
-            <div className="lg:hidden grid grid-cols-3 sm:grid-cols-4 gap-2 sm:gap-4 flex-1">
-              {billboardAlbums.slice(0, 20).map((album) => (
+            {/* Mobile: 18 albums, 3 cols */}
+            <div className="lg:hidden grid grid-cols-3 gap-3">
+              {billboardAlbums.slice(0, 18).map((album) => (
                 <Link
                   key={album.id}
                   href={`/album/${album.spotifyId}`}
                   className="group"
                 >
+                  {/* Album art */}
                   <div className="aspect-square w-full bg-[--border] overflow-hidden mb-2 relative">
                     {album.coverArtUrlLarge || album.coverArtUrl ? (
                       <img
@@ -277,16 +278,19 @@ export default async function Home() {
                         </svg>
                       </div>
                     )}
+                    {/* Billboard rank badge */}
                     {album.billboardRank && (
                       <div className="absolute top-2 left-2 bg-black/80 px-2 py-1 text-[10px] font-bold tracking-wider text-white">
                         #{album.billboardRank}
                       </div>
                     )}
                   </div>
-                  <p className="text-[11px] sm:text-[12px] font-medium truncate group-hover:text-[--muted] transition-colors">
+
+                  {/* Info */}
+                  <p className="text-[11px] font-medium truncate group-hover:text-[--muted] transition-colors">
                     {album.title}
                   </p>
-                  <p className="text-[10px] sm:text-[11px] text-[--muted] truncate">
+                  <p className="text-[10px] text-[--muted] truncate">
                     {album.artistName}
                   </p>
                 </Link>
@@ -294,14 +298,15 @@ export default async function Home() {
             </div>
 
             {/* Desktop: 50 albums, 5 cols */}
-            <div className="hidden lg:grid grid-cols-5 gap-2 flex-1">
+            <div className="hidden lg:grid grid-cols-5 gap-3">
               {billboardAlbums.slice(0, 50).map((album) => (
                 <Link
                   key={album.id}
                   href={`/album/${album.spotifyId}`}
                   className="group"
                 >
-                  <div className="aspect-square w-full bg-[--border] overflow-hidden mb-1 relative">
+                  {/* Album art */}
+                  <div className="aspect-square w-full bg-[--border] overflow-hidden mb-2 relative">
                     {album.coverArtUrlLarge || album.coverArtUrl ? (
                       <img
                         src={album.coverArtUrlLarge || album.coverArtUrl || ''}
@@ -310,21 +315,24 @@ export default async function Home() {
                       />
                     ) : (
                       <div className="w-full h-full flex items-center justify-center text-[--muted]">
-                        <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
+                        <svg className="w-12 h-12" fill="currentColor" viewBox="0 0 24 24">
                           <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 14.5c-2.49 0-4.5-2.01-4.5-4.5S9.51 7.5 12 7.5s4.5 2.01 4.5 4.5-2.01 4.5-4.5 4.5zm0-5.5c-.55 0-1 .45-1 1s.45 1 1 1 1-.45 1-1-.45-1-1-1z"/>
                         </svg>
                       </div>
                     )}
+                    {/* Billboard rank badge */}
                     {album.billboardRank && (
-                      <div className="absolute top-1 left-1 bg-black/80 px-1 py-0.5 text-[8px] font-bold tracking-wider text-white">
+                      <div className="absolute top-2 left-2 bg-black/80 px-2 py-1 text-[10px] font-bold tracking-wider text-white">
                         #{album.billboardRank}
                       </div>
                     )}
                   </div>
-                  <p className="text-[10px] font-medium truncate group-hover:text-[--muted] transition-colors">
+
+                  {/* Info */}
+                  <p className="text-[11px] font-medium truncate group-hover:text-[--muted] transition-colors">
                     {album.title}
                   </p>
-                  <p className="text-[9px] text-[--muted] truncate">
+                  <p className="text-[10px] text-[--muted] truncate">
                     {album.artistName}
                   </p>
                 </Link>
-- 
2.50.1 (Apple Git-155)

